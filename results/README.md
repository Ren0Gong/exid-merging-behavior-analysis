# Generated results

The notebooks create summary tables, raw frame-level PCAD files, diagnostic tables, fitted models, and regression outputs under this directory. Generated artifacts are ignored by default; publish selected small outputs only when dataset licensing and repository-size constraints permit it.
